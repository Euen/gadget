{
  application, gadget_server,
  [
   {description, "Server for Elvis."},
   {vsn, "0.1.0"},
   {applications, [kernel, stdlib, cowboy, lager, ibrowse, gadget, erlydtl, egithub]},
   {mod, {gadget_server, []}},
   {modules,
    [
     gadget_server,
     gadget_server_sup,
     gadget_webhook_handler
    ]
   },
   {registered, []}
  ]
}.
